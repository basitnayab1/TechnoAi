"use client";import{Suspense,useMemo,useRef,useState}from"react";import{useFrame}from"@react-three/fiber";import{Float,Html,useGLTF}from"@react-three/drei";import{Box3,MathUtils,Vector3,type Group}from"three";import{RotateCcw,X}from"lucide-react";import{cn}from"@/lib/utils";import{useExploreOptional}from"./ExploreSequence";import{useCameraFocusOptional}from"./CameraFocus";import{HERO_PLACEMENT,useCompactScene}from"./useCompactScene";import{HOTSPOTS,type HotspotDefinition}from"./hotspots";const DRONE_MODEL_PATH="https://raw.githubusercontent.com/basitnayab1/TechnoAi/cursor/brand-colors-ae31/public/models/drone.glb";const BASE_SCALE=1;const HOVER_SCALE=1.08;const TARGET_SIZE=2.6;function HeroShape(){const outerRef=useRef<Group>(null);const modelRef=useRef<Group>(null);const{scene}=useGLTF(DRONE_MODEL_PATH);const[hovered,setHovered]=useState(false);const explore=useExploreOptional();const focus=useCameraFocusOptional();const compact=useCompactScene();const placement=compact?HERO_PLACEMENT.compact:HERO_PLACEMENT.desktop;const isFocused=Boolean(focus?.activeHotspot);const model=useMemo(()=>{const clone=scene.clone(true);const box=new Box3().setFromObject(clone);const size=box.getSize(new Vector3());const center=box.getCenter(new Vector3());clone.position.sub(center);clone.traverse((child)=>{if ("isMesh"in child&&child.isMesh){child.castShadow=true;child.receiveShadow=true;}});const maxDim=Math.max(size.x,size.y,size.z);const normalizedScale=TARGET_SIZE/maxDim;return{node:clone,normalizedScale};},[scene]);useFrame((_,delta)=>{const outer=outerRef.current;const model3d=modelRef.current;if (!outer||!model3d) return;if (!explore?.isSequencing&&!isFocused){outer.rotation.y+=delta*0.22;}const currentMultiplier=model3d.scale.x/model.normalizedScale;const nextMultiplier=MathUtils.lerp(currentMultiplier,hovered&&!isFocused?HOVER_SCALE:BASE_SCALE,delta*6);const scale=model.normalizedScale*nextMultiplier;model3d.scale.setScalar(scale);});return (<group position={placement.position}scale={placement.scale}><Float speed={isFocused?0:1.6}rotationIntensity={isFocused?0:0.5}floatIntensity={isFocused?0:1.1}><group ref={outerRef}rotation={[0.08,0.55,0]}><group ref={modelRef}scale={model.normalizedScale}onPointerOver={(event)=>{event.stopPropagation();setHovered(true);document.body.style.cursor="pointer";}}onPointerOut={(event)=>{event.stopPropagation();setHovered(false);document.body.style.cursor="auto";}}><primitive object={model.node}/></group>{HOTSPOTS.map((hotspot)=>(<Hotspot key={hotspot.id}hotspot={hotspot}isActive={focus?.activeHotspot===hotspot.id}onSelect={()=> focus?.focusHotspot(hotspot.id)}onReset={()=> focus?.resetView()}onLockOrbit={(locked)=> focus?.setOrbitLocked(locked)}/>))}</group></Float></group>);}function Hotspot({hotspot,isActive,onSelect,onReset,onLockOrbit,}:{hotspot:HotspotDefinition;isActive:boolean;onSelect:()=> void;onReset:()=> void;onLockOrbit:(locked:boolean)=> void;}){const[hovered,setHovered]=useState(false);const highlighted=hovered||isActive;const lock=(next:boolean)=>{onLockOrbit(next);document.body.style.cursor=next?"pointer":"auto";};return (<group position={hotspot.position}><mesh onPointerOver={(event)=>{event.stopPropagation();setHovered(true);lock(true);}}onPointerOut={(event)=>{event.stopPropagation();setHovered(false);lock(false);}}onPointerDown={(event)=>{event.stopPropagation();}}onClick={(event)=>{event.stopPropagation();onSelect();}}><sphereGeometry args={[0.42,16,16]}/><meshBasicMaterial transparent opacity={0}depthWrite={false}/></mesh><Html center sprite occlude={false}zIndexRange={[80,0]}style={{pointerEvents:"auto"}}wrapperClass="pointer-events-auto"><div className="relative flex items-center justify-center"><button type="button"aria-label={`${hotspot.title} hotspot`}aria-expanded={isActive}onPointerDown={(event)=>{event.stopPropagation();}}onClick={(event)=>{event.stopPropagation();event.preventDefault();onSelect();}}onPointerOver={()=>{setHovered(true);lock(true);}}onPointerOut={()=>{setHovered(false);lock(false);}}className={cn("relative flex h-10 w-10 select-none items-center justify-center rounded-full border border-[rgba(0,240,255,0.2)] bg-[rgba(11,15,23,0.75)] font-mono text-xs font-bold tracking-widest text-[#00F0FF] backdrop-blur-sm transition-all duration-300 ease-out lg:text-sm","shadow-[0_0_12px_2px_rgba(0,240,255,0.25)]",highlighted&&"scale-125 border-[#00F0FF] bg-[#00F0FF]/20 text-[#F9FAFB] shadow-[0_0_26px_6px_rgba(0,240,255,0.55)]")}><span className={cn("pointer-events-none absolute -inset-1 rounded-full border border-[#10B981]/50 animate-ping",isActive&&"pause-anim")}/><span className="relative">{hotspot.code}</span></button>{isActive&&(<div role="dialog"aria-label={hotspot.title}className={cn("pointer-events-auto absolute left-1/2 z-50 w-52 max-w-[70vw] -translate-x-1/2 animate-fade-up text-left sm:w-56","rounded-xl border border-[rgba(0,240,255,0.2)] bg-[rgba(11,15,23,0.75)] p-4 shadow-[0_0_25px_rgba(0,240,255,0.2)] backdrop-blur-lg",hotspot.cardPlacement==="up"?"bottom-full mb-3":"top-full mt-3")}><button type="button"aria-label="Close details"onPointerDown={(event)=>{event.stopPropagation();event.preventDefault();onReset();}}onClick={(event)=>{event.stopPropagation();event.preventDefault();}}className="absolute right-2 top-2 flex h-6 w-6 items-center justify-center rounded-full border border-[rgba(255,255,255,0.08)] text-[#9CA3AF] hover:border-[rgba(0,240,255,0.2)] hover:text-[#00F0FF]"><X className="h-3 w-3"/></button><p className="font-mono text-xs font-bold tracking-widest text-[#00F0FF]">{hotspot.code}</p><h4 className="mt-1 pr-6 text-sm font-semibold leading-snug text-[#F9FAFB]">{hotspot.title}</h4><p className="mt-1.5 text-xs leading-relaxed text-[#9CA3AF]">{hotspot.description}</p><button type="button"onPointerDown={(event)=>{event.stopPropagation();event.preventDefault();onReset();}}onClick={(event)=>{event.stopPropagation();event.preventDefault();}}className="mt-3 inline-flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-[#00F0FF] hover:text-[#5AF0FF]"><RotateCcw className="h-3 w-3"/> Reset View </button></div>)}</div></Html></group>);}export function HeroModel(){return (<Suspense fallback={null}><HeroShape/></Suspense>);}useGLTF.preload(DRONE_MODEL_PATH);