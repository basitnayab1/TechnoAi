"use client";import{createContext,useCallback,useContext,useEffect,useMemo,useRef,useState,type MutableRefObject,type ReactNode,}from"react";import{useThree}from"@react-three/fiber";import type{OrbitControls as OrbitControlsImpl}from"three-stdlib";import{gsap}from"@/lib/gsap";import{HERO_PLACEMENT,useCompactScene}from"./useCompactScene";import{DEFAULT_CAMERA,getHotspot,type HotspotId,}from"./hotspots";interface CameraFocusContextValue{activeHotspot:HotspotId|null;isAnimating:boolean;setIsAnimating:(value:boolean)=> void;orbitLocked:boolean;setOrbitLocked:(locked:boolean)=> void;focusHotspot:(id:HotspotId)=> void;resetView:()=> void;shouldIgnoreExplore:()=> boolean;}const CameraFocusContext=createContext<CameraFocusContextValue|null>(null);export function useCameraFocus(){const ctx=useContext(CameraFocusContext);if (!ctx){throw new Error("useCameraFocus must be used within CameraFocusProvider");}return ctx;}export function useCameraFocusOptional(){return useContext(CameraFocusContext);}export function CameraFocusProvider({children}:{children:ReactNode}){const[activeHotspot,setActiveHotspot]=useState<HotspotId|null>(null);const[isAnimating,setIsAnimating]=useState(false);const[orbitLocked,setOrbitLocked]=useState(false);const ignoreExploreUntil=useRef(0);const focusHotspot=useCallback((id:HotspotId)=>{setActiveHotspot((current)=>(current===id?null:id));},[]);const resetView=useCallback(()=>{setActiveHotspot(null);ignoreExploreUntil.current=Date.now()+500;},[]);const shouldIgnoreExplore=useCallback(()=> Date.now()< ignoreExploreUntil.current,[]);const value=useMemo<CameraFocusContextValue>(()=>({activeHotspot,isAnimating,setIsAnimating,orbitLocked,setOrbitLocked,focusHotspot,resetView,shouldIgnoreExplore,}),[activeHotspot,focusHotspot,isAnimating,orbitLocked,resetView,shouldIgnoreExplore,]);return (<CameraFocusContext.Provider value={value}>{children}</CameraFocusContext.Provider>);}function worldFromLocal(local:[number,number,number],placement:(typeof HERO_PLACEMENT)["compact"|"desktop"]):[number,number,number]{return[placement.position[0]+local[0]*placement.scale,placement.position[1]+local[1]*placement.scale,placement.position[2]+local[2]*placement.scale,];}interface CameraFocusRigProps{controlsRef:MutableRefObject<OrbitControlsImpl|null>;}export function CameraFocusRig({controlsRef}:CameraFocusRigProps){const{camera}=useThree();const{activeHotspot,setIsAnimating}=useCameraFocus();const compact=useCompactScene();const timelineRef=useRef<ReturnType<typeof gsap.timeline>|null>(null);const didMount=useRef(false);const placement=compact?HERO_PLACEMENT.compact:HERO_PLACEMENT.desktop;const restLookAt=useMemo(():[number,number,number]=>[placement.position[0],placement.position[1]+0.15,placement.position[2],],[placement]);useEffect(()=>{if (!didMount.current){didMount.current=true;if (activeHotspot===null) return;}const controls=controlsRef.current;const hotspot=getHotspot(activeHotspot);timelineRef.current?.kill();setIsAnimating(true);const lookTarget={x:restLookAt[0],y:restLookAt[1],z:restLookAt[2],};const camTarget={x:DEFAULT_CAMERA[0],y:compact?0.35:DEFAULT_CAMERA[1],z:DEFAULT_CAMERA[2],};if (hotspot){const offsetScale=compact?0.82:1;const focus=worldFromLocal(hotspot.position,placement);const cam=worldFromLocal([hotspot.position[0]+hotspot.cameraOffset[0]*offsetScale,hotspot.position[1]+hotspot.cameraOffset[1]*offsetScale,hotspot.position[2]+hotspot.cameraOffset[2]*offsetScale,],placement);lookTarget.x=focus[0];lookTarget.y=focus[1];lookTarget.z=focus[2];camTarget.x=cam[0];camTarget.y=cam[1];camTarget.z=cam[2];}const look=()=>{camera.lookAt(lookTarget.x,lookTarget.y,lookTarget.z);controls?.target.set(lookTarget.x,lookTarget.y,lookTarget.z);controls?.update();};const tl=gsap.timeline({defaults:{ease:"power3.inOut"},onComplete:()=>{look();setIsAnimating(false);},});tl.to(camera.position,{...camTarget,duration:1.25,onUpdate:look},0);if (controls){tl.to(controls.target,{...lookTarget,duration:1.25,onUpdate:look},0);}timelineRef.current=tl;return ()=>{timelineRef.current?.kill();timelineRef.current=null;};},[activeHotspot,camera,compact,controlsRef,placement,restLookAt,setIsAnimating,]);return null;}