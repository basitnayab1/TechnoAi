"use client";import{motion,useReducedMotion}from"framer-motion";import{Banknote,Plane,Bot,Satellite,Factory,ShieldCheck,type LucideIcon,}from"lucide-react";import{Container}from"@/components/ui/Container";import{SectionHeading}from"@/components/ui/SectionHeading";import{fadeUp,springSoft}from"@/lib/motion";import{siteContent}from"@/lib/content";import{cn,glassHover,glassPad,glassPanel}from"@/lib/utils";const FEATURE_ICONS:LucideIcon[]=[Banknote,Plane,Bot,Satellite,Factory,ShieldCheck,];const features=siteContent.solutions.map((item,index)=>({...item,icon:FEATURE_ICONS[index]??ShieldCheck,}));function FeatureCard({feature,index,}:{feature:(typeof features)[number];index:number;}){const Icon=feature.icon;const reduce=useReducedMotion();return (<motion.div variants={fadeUp}initial="initial"whileInView="animate"viewport={{once:true,amount:0.25}}transition={{...springSoft,delay:index*0.12}}whileHover={reduce?undefined:{y:-6,scale:1.02}}className={cn(glassPanel,glassHover,glassPad,"group relative flex min-w-0 flex-col gap-4 will-change-transform")}><div className="flex items-center justify-between gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-primary/10 text-primary-300 transition-colors group-hover:bg-primary/20"><Icon className="h-5 w-5"/></span><span className="font-mono text-xs text-foreground/40">{feature.code}</span></div><h3 className="text-lg font-semibold">{feature.title}</h3><p className="mt-1 text-sm text-[#9CA3AF]">{feature.description}</p></motion.div>);}export function Features(){const{eyebrow,title,description}=siteContent.sections.solutions;return (<section id="solutions"className="relative scroll-mt-8 py-24 sm:py-32"><Container className="flex flex-col gap-16"><SectionHeading eyebrow={eyebrow}title={title}description={description}/><div className="grid min-w-0 gap-6 sm:grid-cols-2 lg:grid-cols-3">{features.map((feature,index)=>(<FeatureCard key={feature.code}feature={feature}index={index}/>))}</div></Container></section>);}